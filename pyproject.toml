[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "chiptrace"
dynamic = ["version"]
description = "万众一芯开放验证团队的芯片行业 Trace 治理框架"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{name = "万众一芯开放验证团队"}]
requires-python = ">=3.10"
dependencies = []
keywords = ["agent", "trace", "trajectory", "dataset", "sqlite"]
classifiers = [
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Operating System :: POSIX :: Linux",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Database",
  "Topic :: Software Development :: Libraries :: Python Modules",
]

[project.optional-dependencies]
performance = ["orjson>=3.10", "zstandard>=0.22"]

[project.scripts]
chiptrace = "chiptrace.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = {attr = "chiptrace.__version__"}

[tool.setuptools.package-data]
chiptrace = ["py.typed"]
"chiptrace.specs" = ["*.json", "*.yaml", "*.sql"]
