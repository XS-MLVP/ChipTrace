[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "chiptrace-governance"
dynamic = ["version"]
description = "万众一芯开放验证团队的芯片行业 Trace 治理框架"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{name = "万众一芯开放验证团队"}]
requires-python = ">=3.10"
dependencies = []
keywords = ["agent", "trace", "trajectory", "dataset", "sqlite"]
classifiers = [
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Operating System :: POSIX :: Linux",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Database",
  "Topic :: Software Development :: Libraries :: Python Modules",
]

[project.optional-dependencies]
performance = ["orjson>=3.10", "zstandard>=0.22"]

[project.scripts]
chiptrace = "trace_pipeline.cli:main"
trace-pipeline = "trace_pipeline.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = {attr = "trace_pipeline.__version__"}

[tool.setuptools.package-data]
trace_pipeline = ["py.typed"]
"trace_pipeline.specs" = ["*.json", "*.yaml", "*.sql"]
